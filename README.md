# pyocr

Written for Python 3.9 or later versions.

It extracts text from the fixed area of image files contained in the input fold-
er and saves it to a CSV file.

The input directory and output directory can be changed to `INPUT_DIR` and `OUT-
PUT_DIR` respectively in the `.env` file.
